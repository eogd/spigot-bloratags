<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.blorasoft</groupId>
    <artifactId>bloratags</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>BloraTags</name>

    <properties>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <spigot.version>1.21-R0.1-SNAPSHOT</spigot.version> <!-- Use Spigot 1.21 -->
        <minimessage.version>4.17.0</minimessage.version> <!-- Check for latest Kyori Adventure -->
        <papi.version>2.11.6</papi.version> <!-- Check for latest PAPI -->
        <vault.version>1.7.3</vault.version> <!-- Check for latest Vault -->
        <hikaricp.version>5.1.0</hikaricp.version> <!-- Check for latest HikariCP -->
        <mysql.connector.version>8.0.33</mysql.connector.version> <!-- Check for latest MySQL Connector/J -->
        <sqlite.jdbc.version>3.45.3.0</sqlite.jdbc.version> <!-- Check for latest SQLite JDBC -->
        <slf4j.version>1.7.36</slf4j.version> <!-- SLF4J version (often compatible with newer versions too) -->
        <annotations.version>24.1.0</annotations.version> <!-- JetBrains Annotations -->
    </properties>

    <repositories>
        <!-- Spigot Repo -->
        <repository>
            <id>spigot-repo</id>
            <url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
        </repository>
        <!-- PlaceholderAPI Repo -->
        <repository>
            <id>placeholderapi</id>
            <url>https://repo.extendedclip.com/content/repositories/placeholderapi/</url>
        </repository>
        <!-- Vault Repo -->
        <repository>
            <id>jitpack.io</id>
            <url>https://jitpack.io</url>
        </repository>
        <!-- Sonatype OSS Snapshots (for potential snapshot dependencies) -->
        <repository>
            <id>sonatype</id>
            <url>https://oss.sonatype.org/content/groups/public/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- Spigot API -->
        <dependency>
            <groupId>org.spigotmc</groupId>
            <artifactId>spigot-api</artifactId>
            <version>1.21.1-R0.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
        <!-- MiniMessage API (Kyori Adventure) - Needs to be shaded -->
        <dependency>
            <groupId>net.kyori</groupId>
            <artifactId>adventure-api</artifactId>
            <version>4.17.0</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>net.kyori</groupId>
            <artifactId>adventure-platform-bukkit</artifactId>
            <version>4.3.3</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>net.kyori</groupId>
            <artifactId>adventure-text-minimessage</artifactId>
            <version>4.17.0</version>
            <scope>compile</scope>
        </dependency>
        <!-- PlaceholderAPI -->
        <dependency>
            <groupId>me.clip</groupId>
            <artifactId>placeholderapi</artifactId>
            <version>${papi.version}</version>
            <scope>provided</scope>
        </dependency>
        <!-- Vault API -->
        <dependency>
            <groupId>com.github.MilkBowl</groupId>
            <artifactId>VaultAPI</artifactId>
            <version>1.7.1</version>
            <scope>provided</scope>
        </dependency>
        <!-- HikariCP (Connection Pooling) - Needs to be shaded -->
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
            <version>${hikaricp.version}</version>
            <scope>compile</scope>
        </dependency>
        <!-- Database Drivers (Shade these) -->
        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
            <version>8.0.33</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>3.42.0.0</version>
            <scope>compile</scope>
        </dependency>
        <!-- SLF4J API (Required by HikariCP) - Needs to be shaded -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>2.0.9</version>
            <scope>compile</scope>
        </dependency>
        <!-- JetBrains Annotations (Optional but helpful) -->
        <dependency>
            <groupId>org.jetbrains</groupId>
            <artifactId>annotations</artifactId>
            <version>24.1.0</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <defaultGoal>clean package</defaultGoal>
        <plugins>
            <!-- Compiler Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version> <!-- Use a recent version -->
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>
            <!-- Shade Plugin (for bundling and relocating dependencies) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.1</version> <!-- Use a recent version -->
                <configuration>
                    <relocations>
                        <!-- Relocate Adventure API to avoid conflicts with other plugins -->
                        <relocation>
                            <pattern>net.kyori</pattern>
                            <shadedPattern>com.blorasoft.bloratags.libs.kyori</shadedPattern>
                        </relocation>
                        <!-- Relocate HikariCP -->
                        <relocation>
                            <pattern>com.zaxxer.hikari</pattern>
                            <shadedPattern>com.blorasoft.bloratags.libs.hikaricp</shadedPattern>
                        </relocation>
                        <!-- Relocate SLF4J -->
                        <relocation>
                            <pattern>org.slf4j</pattern>
                            <shadedPattern>com.blorasoft.bloratags.libs.slf4j</shadedPattern>
                        </relocation>
                        <!-- Relocate MySQL Connector -->
                        <relocation>
                            <pattern>com.mysql</pattern>
                            <shadedPattern>com.blorasoft.bloratags.libs.mysql</shadedPattern>
                        </relocation>
                        <!-- Relocate SQLite JDBC -->
                        <relocation>
                            <pattern>org.sqlite</pattern>
                            <shadedPattern>com.blorasoft.bloratags.libs.sqlite</shadedPattern>
                        </relocation>
                    </relocations>
                    <createDependencyReducedPom>false</createDependencyReducedPom>
                    <filters>
                        <filter>
                            <!-- Exclude unnecessary files from shaded jar -->
                            <artifact>*:*</artifact>
                            <excludes>
                                <exclude>META-INF/*.SF</exclude>
                                <exclude>META-INF/*.DSA</exclude>
                                <exclude>META-INF/*.RSA</exclude>
                                <exclude>META-INF/LICENSE*</exclude>
                                <exclude>META-INF/NOTICE*</exclude>
                                <exclude>licenses/**</exclude>
                            </excludes>
                        </filter>
                    </filters>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering> <!-- Enables replacing ${...} placeholders in plugin.yml -->
            </resource>
        </resources>
    </build>
</project>